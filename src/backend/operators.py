"""
NB : Dans la fonction calculate dans app.py on force un float lorsqu'on l'utilise afin d'avoir un retour constant
"""

def add(a,b):
    """
    Réalise l'addition 
    Paramètre : a, b => ici ce n'est pas un type en particulier 
    Retourne: le resultat de l'addition a+b (peut être un float si on force a et b comme dans le cas de )
    """
    return a + b

def subtract(a,b):
    """
    Réalise la soustraction 
    Paramètre : a, b => ici ce n'est pas un type en particulier 
    Retourne: le resultat de la soustraction a-b (peut être un float si on force a et b comme dans le cas de )
    """
    return a - b

def multiply(a,b):
    """
    Réalise la multiplication 
    Paramètre : a, b => ici ce n'est pas un type en particulier 
    Retourne: le resultat de l'addition a*b (peut être un float si on force a et b comme dans le cas de )
    """
    return a * b

def divide(a,b):
    """
    Réalise la division 
    Paramètre : a, b => ici ce n'est pas un type en particulier 
    Retourne: le resultat de la division a/b (peut être un float si on force a et b comme dans le cas de )
    """
    return a / b
